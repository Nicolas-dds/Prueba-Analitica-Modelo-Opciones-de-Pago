"""API HTTP de `sistema-agentico` (tarea 17.3).

Expone el `Orchestrator`/`ReactiveRunner`/`ProactiveBatchRunner` ya existentes como
un servicio HTTP nuevo (FastAPI), fuera del alcance original de `design.md` (ver
tarea 17 de `.kiro/specs/sistema-agentico-cobranza/tasks.md`).
"""
